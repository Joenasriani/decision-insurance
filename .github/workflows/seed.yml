name: Decision Insurance Build Seed

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived_seed:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Initial repository seed completed."
